services:
  - type: web
    name: seerah-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_GEMINI_API_KEY
        sync: false
