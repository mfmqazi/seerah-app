services:
  - type: web
    name: seerah-app
    env: docker
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_GEMINI_API_KEY
        sync: false
